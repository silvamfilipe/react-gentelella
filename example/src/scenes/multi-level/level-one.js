import React, {Fragment} from 'react';
import { Page, PageTitle } from 'react-gentelella';

const LevelOne = () => (
    <Page>
        <PageTitle title={<Fragment>Multilevel Menu 1 <small>Page to demonstrate multilevel menu</small></Fragment>}/>
    </Page>
);

export default LevelOne;
